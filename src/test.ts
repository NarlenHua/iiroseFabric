// async function f() { w(), await async function () { let e = document.createElement("script"); e.src = "https://cdn.bootcdn.net/ajax/libs/eruda/3.1.0/eruda.min.js", document.body.append(e), localStorage.getItem("allowEruda") == (!0).toString() ? e.onload = () => { eruda.init(), eruda.position({ x: window.innerWidth - 100, y: window.innerHeight - 50 }) } : localStorage.setItem("allowEruda", (!1).toString()) }(), window.iirosesave = () => { Probe.init.pako || (Probe.init.pako = 1, Utils.getScript("lib/js/app/server/pako.js")); try { Utils.service.saveStatus(0) } catch (c) { sendBug(c, "saveStatus") } for (var e, t = {}, o = 0, n = localStorage.length; o < n; ++o)t[e = localStorage.key(o)] = localStorage.getItem(e); var l = JSON.stringify(t), a = [(a = new Date).getFullYear(), Utils.smallTools.zeroFill(a.getMonth() + 1), Utils.smallTools.zeroFill(a.getDate())].join("-") + "_" + [Utils.smallTools.zeroFill(a.getHours()), Utils.smallTools.zeroFill(a.getMinutes()), Utils.smallTools.zeroFill(a.getSeconds())].join("-"), i = uid + "_" + a + ".bak.iirose"; a = pako.gzip(l, { level: 9 }); (l = new Uint8Array(a.length + 3))[0] = 2, l[1] = 33, l[a.length + 2] = 77, l.set(a, 2), l = new Blob([l]), 5 == device ? Utils.blobToDataURL(l, (function (e) { Main.saveFile(e, i) })) : Utils.service.downloadBlob(l, i) }, window.iiroserepair = () => { localStorage.setItem("functionPos", "0#"), location.reload() }, window.openconsole = () => { eruda.init() }, window.closeconsole = () => { eruda.destroy() }, window.clearfunctionPos = () => { localStorage.setItem("functionPos", "0#"), window.addEventListener("unload", (e => { localStorage.setItem("functionPos", "0#"), console.log("界面重载，清理", e) })) }; let e = localStorage.getItem("externalResources2"), t = []; if (null != e) { t = JSON.parse(e), console.log("提前注入的数据", t); for (let e = 0; e < t.length; e++)if (0 == t[e][2]) { let o = document.createElement("script"); o.src = t[e][1], document.body.append(o) } } } await f();